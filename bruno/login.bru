meta {
  name: login
  type: http
  seq: 2
}

post {
  url: http://127.0.0.1:3050/api/login
  body: json
  auth: none
}

body:json {
  {
    "username": "{{username}}",
    "password": "{{password}}"
  }
}

vars:pre-request {
  username: admin
  password: hunter2
}

script:post-response {
  bru.setVar("token", res.body.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
